# API Gateway Configuration

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-min-32-chars
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# SMTP Configuration (Gmail)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Application Configuration
NODE_ENV=development
PORT=3005

# Admin Secret Code
ADMIN_SECRET=admin-secret

# Security Configuration
MAX_LOGIN_ATTEMPTS=5
LOCKOUT_DURATION=900000
# 15 minutes in milliseconds

# Database Configuration (MongoDB)
MONGODB_URI=mongodb://localhost:27017/server-shield
# Or use MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/server-shield

# Instructions:
# 1. For Gmail SMTP: Enable 2-factor authentication, then create an App Password at https://myaccount.google.com/apppasswords
# 2. Replace SMTP_USER with your Gmail address
# 3. Replace SMTP_PASS with the generated App Password
# 4. Change JWT_SECRET to a random 32+ character string
# 5. For MongoDB: Install MongoDB locally or use MongoDB Atlas (cloud)
